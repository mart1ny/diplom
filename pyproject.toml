[tool.pytest.ini_options]
addopts = "-ra"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = [
  "frontend/dist",
  "frontend/node_modules",
  "results",
  "uploads",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82", "I"]
